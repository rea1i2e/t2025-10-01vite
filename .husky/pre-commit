echo "🔍 ビルドテストを実行中..."
npm run validate:build

if [ $? -ne 0 ]; then
  echo "❌ ビルドテストでエラーが検出されました。コミットを中止します。"
  echo "💡 EJSファイルの構文エラーやHTMLの構造エラーを確認してください。"
  exit 1
fi

echo "✅ すべてのバリデーション完了"
echo "✅ HTMLバリデーション完了"