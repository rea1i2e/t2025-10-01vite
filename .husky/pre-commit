echo "🏗️ ビルド＋HTML検証を実行中（validate:build）..."
npm run validate:build || {
  echo "❌ validate:build でエラーが検出されました。コミットを中止します。"
  echo "💡 vite build・scripts/after-build.mjs・HTML検証を確認してください。"
  exit 1
}

echo "✅ validate:build 完了"
